@model IEnumerable<AssetSystem.Models.AssetDeclaration>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AssertType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AssertPrefix)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AssertType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AssertPrefix)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AssetId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AssetId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AssetId })
        </td>
    </tr>
}

</table>
